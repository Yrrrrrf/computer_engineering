import numpy as np


arr = np.array([13, 6, 3, 5, 10])
# print(np.sort(arr))
# print(np.argmax(arr))
# print(np.max(arr))
print(arr.shape)
np.expand_dims(arr, axis=0)
print(arr.shape)

# a = np.array([[7, 8], [2, 1], [2, 4]])
# print(a.shape)